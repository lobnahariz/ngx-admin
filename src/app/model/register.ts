export class Register {
  constructor (
               public username?: string,
              public password?: string,
              public repassword?: string,
               public email?: string,
  ) {
  }
}

